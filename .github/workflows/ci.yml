name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck (all workspaces, including rider-web)
        run: pnpm typecheck

      - name: Build (all workspaces)
        run: pnpm build

      - name: Protocol unit tests (money + FSM)
        run: pnpm --filter @chalo/protocol test

      - name: Core unit tests
        run: pnpm --filter @chalo/core test

      # Self-contained live suite: boots a real server on an in-memory PGlite
      # cluster and drives the full negotiation/dispatch/settlement flows.
      - name: Core verification suite (68-assertion live flows)
        run: pnpm --filter @chalo/core test:verify
